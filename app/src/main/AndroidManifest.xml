<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="day.nice.happyvideo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 获取网络状态 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 网络通信 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 获取设备信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- 获取MAC地址 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 读写sdcard，storage等等 -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:name="day.nice.happyvideo.App"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.LightStatusBar">
        <activity android:name="day.nice.happyvideo.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.abc.packagelibrary.WebTwoActivity"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true">
        </activity>
        <activity
            android:name="day.nice.happyvideo.MainActivity"
            android:exported="true"
            android:label="@string/app_name">


            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:name="day.nice.happyvideo.MovieDetailActivity"
            android:label=""
            android:theme="@style/AppTheme.Movie" />
        <activity
            android:name="day.nice.happyvideo.PlayActivity"
            android:label=""
            android:screenOrientation="landscape" />
        <activity android:name="day.nice.happyvideo.interesting.InterestingActivity" />
        <activity android:name="day.nice.happyvideo.SearchActivity" />
        <activity
            android:name="day.nice.happyvideo.ResultActivity"
            android:label="@string/result_title" />
        <activity android:name="day.nice.happyvideo.RelatedActivity" />
        <activity android:name="day.nice.happyvideo.FindInterestingActivity" />
        <activity android:name="day.nice.happyvideo.VideoListActivity" />
        <activity android:name="day.nice.happyvideo.VideoAuthorActivity" />
        <activity
            android:name="day.nice.happyvideo.SettingsActivity"
            android:label="@string/settings" />
    </application>

</manifest>